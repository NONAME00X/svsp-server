<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.cxy.svspcxy.mapper.UserMapper">
    <select id="findByAccount" resultType="edu.cxy.svspcxy.entity.User">
        SELECT u.id,u.account,u.password,u.avatar,u.state,r.name role
        FROM t_user u,t_user_role ur,t_role r
        WHERE u.id = ur.uid AND ur.rid = r.id AND account = #{account}
    </select>

    <select id="getUserInfo" resultType="edu.cxy.svspcxy.entity.User">
        SELECT * FROM t_user WHERE id = #{uid}
    </select>
    <update id="changeAvatar">
        update t_user set avatar = #{avatarUrl} where id = #{uid}
    </update>

    <update id="changePwd">
        update t_user set password = #{newPwd} where id = #{uid}
    </update>
</mapper>
